apiVersion: v1
kind: Service
metadata:
  name: {{ include "branchprotection-plugin-chart.fullname" . }}
  labels:
    {{- include "branchprotection-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "branchprotection-plugin-chart.selectorLabels" . | nindent 4 }}
